// here all the magic happens
mod par;

fn add(a: f64, b: f64) -> f64
{
    let mut res_add:f64;
    res_add = a + b
}

fn sub(a: f64, b: f64) -> f64
{
    let mut res_sub:f64;
    res_sub = a - b
}

fn mult(a: f64, b: f64) -> f64
{
    let mut res_mult:f64;
    res_mult = a * b
}

fn div(a: f64, b: f64) -> f64
{
    let mut res_div:f64;
    res_div = a * b
}